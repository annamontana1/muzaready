# Database Configuration
# =====================
# 
# Supabase provides two connection types:
# 
# 1. DATABASE_URL (Connection Pooler - Port 6543)
#    - Recommended for application queries
#    - Uses PgBouncer for connection pooling
#    - Better performance under high traffic
#    - Format: postgresql://postgres.[project-ref]:[password]@aws-0-[region].pooler.supabase.com:6543/postgres
# 
# 2. DIRECT_URL (Direct Connection - Port 5432)
#    - Required for Prisma migrations
#    - Required for reliable health checks
#    - Direct connection to PostgreSQL
#    - Format: postgresql://postgres.[project-ref]:[password]@aws-0-[region].pooler.supabase.com:5432/postgres
#
# Both URLs should include SSL mode: ?sslmode=require or ?pgbouncer=true for pooler
#

# Pooled connection (recommended for app queries)
DATABASE_URL="postgresql://postgres:password@localhost:6543/mydb?pgbouncer=true&connection_limit=1"

# Direct connection (required for migrations and health checks)
DIRECT_URL="postgresql://postgres:password@localhost:5432/mydb?sslmode=require"

# QUICK WORKAROUND:
# If you experience database connection issues, temporarily set DATABASE_URL
# to the same value as DIRECT_URL (port 5432). This bypasses the pooler but
# ensures immediate connectivity.
# 
# Example:
# DATABASE_URL="postgresql://postgres:password@localhost:5432/mydb?sslmode=require"
# DIRECT_URL="postgresql://postgres:password@localhost:5432/mydb?sslmode=require"

# GoPay Payment Gateway
GOPAY_GOID=""
GOPAY_CLIENT_ID=""
GOPAY_CLIENT_SECRET=""
GOPAY_TEST_MODE="true"

# Email Configuration (Optional)
SMTP_HOST=""
SMTP_PORT="587"
SMTP_USER=""
SMTP_PASSWORD=""
EMAIL_FROM="noreply@example.com"

# Admin Authentication
ADMIN_JWT_SECRET="change-this-in-production"

# Vercel Protection Bypass (for automated testing)
VERCEL_AUTOMATION_BYPASS_SECRET=""

# Application URL
NEXT_PUBLIC_APP_URL="http://localhost:3000"
